$(document).ready(function() {
	/* parallax code */
	$('.parallax').parallax(); 

	/* scrollspy code */
	$('.scrollspy').scrollSpy();

	/* slider code */
	$('.slider').slider({full_width: true});

	/* sidenav code */
	$(".button-collapse").sideNav();
});